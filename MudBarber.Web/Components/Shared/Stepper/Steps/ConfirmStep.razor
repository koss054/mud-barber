@using MudBarber.Web.Models

@* TODO: add onclick to MudTextFields which open the step responsible for the data in it *@

<div class="confirm-step">
    <MudText Align="Align.Center" Typo="Typo.h6">
        Confirm your booking
    </MudText>
    <MudTextField 
        Class="mb-4"
        Text="@ServiceName"
        Label="Service"
        Variant="Variant.Outlined"
        Typo="Typo.body1"
        ReadOnly="true"
        T="string"
        @onclick="@(() => Console.WriteLine("Service clicked"))" 
    />
    <MudTextField
        Class="mb-4"
        Text="@BarberName"
        Label="Barber"
        Variant="Variant.Outlined"
        Typo="Typo.body1"
        ReadOnly="true"
        T="string"
    />
    <MudTextField
        Class="mb-4"
        Text="@Date"
        Label="Date"
        Variant="Variant.Outlined"
        Typo="Typo.body1"
        ReadOnly="true"
        T="string"
    />
    <MudTextField
        Text="@Time"
        Label="Time"
        Variant="Variant.Outlined"
        Typo="Typo.body1"
        ReadOnly="true"
        T="string"
    />
</div>

@code {
    [Parameter, EditorRequired]
    public BookingModel Model { get; set; } = default!;

    private const string _emptyFieldPlaceholder = "---";

    private string ServiceName =>
        Model.Service?.Name ?? _emptyFieldPlaceholder;

    private string BarberName
        => Model.Barber is not null
            ? $"{Model.Barber.FirstName} {Model.Barber.LastName}"
            : _emptyFieldPlaceholder;

    private string Date =>
        Model.Date is not null
            ? Model.Date!.Value.ToString("yyyy-MM-dd")
            : _emptyFieldPlaceholder;

    private string Time =>
        Model.Time is not null
            ? $"{Model.Time!.Value.ToString("HH:mm")} - {Model.Time!.Value.AddMinutes(Model.Service!.Minutes).ToString("HH:mm")}"
            : _emptyFieldPlaceholder;
}
