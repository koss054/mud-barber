@using MudBarber.Web.Components.Shared.Buttons
@inherits LayoutComponentBase

<MudThemeProvider @bind-IsDarkMode="_darkMode" Theme="_theme" />
<MudSwitch @bind-Value="_darkMode" Color="Color.Primary" Class="ma-4" T="bool" Label="Toggle Dark/Light Mode" />

<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar>
        MudBarber
    </MudAppBar>

    <MudDrawer Open="true">
        <DisplayModeToggle @bind-DarkMode="_darkMode" />
    </MudDrawer>

    <MudMainContent>
        @Body
    </MudMainContent>
</MudLayout>

@code {
    private MudTheme _theme = new();
    private bool _darkMode = true; 
}